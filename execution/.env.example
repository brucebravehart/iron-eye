# Runtime mode: paper | live
TRADING_MODE=paper

# Optional signal source path
SIGNAL_FILE=signals/latest.json

# Broker selection key from config/brokers.example.yaml
BROKER_NAME=paper

# API keys (example names)
BROKER_API_KEY=
BROKER_API_SECRET=
